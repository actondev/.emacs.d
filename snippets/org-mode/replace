# -*- mode: snippet -*-
# name: org-src-replace
# key: :replace
# --
:replace `(mapconcat
		'identity
		(completing-read "Local variables: " (mapcar
			  (lambda (x)
			    (symbol-name (car x)))
			  (aod/local-variables))
		:marked-candidates t)
		" ")`